# Copy this file to `frontend/.streamlit/secrets.toml` and update values
# These secrets are available in Streamlit Cloud under App settings > Secrets

# Backend FastAPI base URL
backend_url = "https://your-backend.example.com"
